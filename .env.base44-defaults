# Base44 placeholder defaults — let the app boot before all user secrets land.
# Every value here is overridden by /run/base44/app.env (loaded LAST in compose),
# so real dashboard secrets always win. Do NOT put real credentials here.

# JWT signing secret (required at boot). Dev-only random value; override via
# the dashboard SESSION_SECRET secret for a stable signing key.
SESSION_SECRET=base44-dev-session-secret-change-me-9f3a7c1e4b2d
